apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: mediacms-full
  namespace: mediacms
  labels:
    app.kubernetes.io/name: mediacms
    app.kubernetes.io/part-of: mediacms
    app.kubernetes.io/managed-by: argocd
    app.kubernetes.io/component: build
spec:
  runPolicy: Serial
  source:
    type: Git
    git:
      uri: https://github.com/mediacms-io/mediacms.git
      ref: main
    contextDir: .
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile.openshift.full
      buildArgs:
        - name: DEVELOPMENT_MODE
          value: "False"
      from:
        kind: ImageStreamTag
        name: mediacms-base:latest
  output:
    to:
      kind: ImageStreamTag
      name: mediacms-full:latest
  resources:
    limits:
      cpu: "2000m"
      memory: 4Gi
    requests:
      cpu: "1000m"
      memory: 2Gi
  triggers:
    - type: ConfigChange
    - type: ImageChange
      imageChange:
        from:
          kind: ImageStreamTag
          name: mediacms-base:latest
    - type: GitHub
      github:
        secret: github-webhook-secret
      # Note: Webhook secret must be created separately
      # oc create secret generic github-webhook-secret --from-literal=WebHookSecretKey=your-secret -n mediacms
